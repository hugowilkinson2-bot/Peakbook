import { AppShell } from "@/components/app-shell";
import { RouteCard } from "@/components/route-card";
import { routes } from "@/lib/routes";
import { Search, SlidersHorizontal } from "lucide-react";

export default function RoutesPage() { return <AppShell><header className="mb-7"><p className="text-sm font-medium text-moss">Tu archivo de montaña</p><h1 className="mt-1 text-4xl font-semibold tracking-[-.05em]">Mis rutas</h1></header><div className="mb-6 flex gap-3"><label className="flex h-12 flex-1 items-center gap-3 rounded-2xl bg-white px-4 shadow-sm"><Search size={18} className="text-moss"/><input aria-label="Buscar rutas" placeholder="Buscar cima o lugar" className="min-w-0 flex-1 bg-transparent text-sm placeholder:text-ink/35"/></label><button aria-label="Filtrar rutas" className="grid h-12 w-12 place-items-center rounded-2xl bg-forest text-lime"><SlidersHorizontal size={19}/></button></div><div className="mb-6 flex gap-2 overflow-x-auto pb-1 text-xs font-semibold"><button className="whitespace-nowrap rounded-full bg-ink px-4 py-2 text-white">Todas · 12</button><button className="whitespace-nowrap rounded-full bg-white px-4 py-2 text-ink/55">Completadas · 9</button><button className="whitespace-nowrap rounded-full bg-white px-4 py-2 text-ink/55">Guardadas · 3</button></div><div className="space-y-5">{routes.map(route=><RouteCard key={route.id} route={route} compact/>)}</div></AppShell>; }
